{
  "name": "utm-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous Drone Traffic Management (UTM) system - simulation monorepo",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "test": "pnpm -r test",
    "dev": "pnpm build && concurrently -n api,web -c blue,magenta \"pnpm --filter @utm/api dev\" \"pnpm --filter @utm/web dev\"",
    "start": "pnpm --filter @utm/api start",
    "e2e": "node scripts/smoke.mjs",
    "check": "pnpm build && pnpm typecheck && pnpm lint && pnpm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.17.0",
    "playwright-core": "^1.49.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
